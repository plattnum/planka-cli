# CLAUDE.md

This project uses [AGENTS.md](AGENTS.md) as the canonical guide for agents and contributors. See that file for workspace layout, build/test commands, design rules, exit codes, auth, and API quirks.

For Planka operations via the `plnk` CLI, use the `plnk-cli` skill at [`skills/plnk-cli/SKILL.md`](skills/plnk-cli/SKILL.md).
